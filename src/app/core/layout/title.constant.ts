export class TitleConstant {

    /**
     * Use within the route data.title to tell the routing service to skip applying the route
     * title or default.
     *
     * This means that the title will remain the same unless modified manually.
     */
    static readonly NO_ROUTE_TITLE: string = '__NO_ROUTE_TITLE__';
}
